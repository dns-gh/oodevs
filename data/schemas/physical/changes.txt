Ravitaillement.xml :
time au lieu de : constitution-time
                  loading-time
                  unloading-time
pour simplifier la lecture car ces trois sont lus par la même fonction.


Il faudrait changer le fichier capteurs.xml au niveau des base-distance car en fait dans l'ancien on avait
dans l'ordre : DD, DR, DI et cet ordre ne doit pas changer dans la lecture sinon ça foire.
pour l'instant j'ai laissé tel quel mais il faudrait voir à changer


Dans le PHY_SensorTypeAgent on fait appel à un template pour pouvoir initialiser les facteurs.
Or dans celui ci il liste tous les  "containers" différents en fonction du type
Vu que c'est une fonction générique et qu'on a mis des noms différents pour chaque catégorie
( "precipitattion-modifier", "visibility-modifier"... ) j'ai changé ceux ci en "distance-modifier" 
pour garder l'aspect générique de cette fonction.( j'ai aussi changé les fichiers xsd et xslt ).

fichiers à revoir:
    -PHY_SensorTypeAgent.cpp( classe template + fonction ReadTerrainModifier )

revoir absolument la fonction PHY_RadarType::ReadTime pour voir si pas d'erreurs( avec base-time et command-post-time ).

j'ai changé les Archive en xeumeuleu du fichier PHY_Meteo et ses dépendances. Mais pas la structure du fichier xml.

Le fichier PHY_DotationStockContainer.cpp a été aussi changé par contre
je ne suis pas arrivé à trouver le fichier xml correspondant donc
j'ai juste changé la libraire de lecture du xml.

voilà c'est à à peu prés tout.
Je n'ai pas eu beaucoup de temps pour tester et je n'ai pas réussi à trouver d'ou venaient les erreurs
que j'avais car en regardant le fichier xml correspondant il n'y avait pas de problème!
bref! j'espère que je n'ai pas fait trop de bétises :D
bon courage pour la suite ;)