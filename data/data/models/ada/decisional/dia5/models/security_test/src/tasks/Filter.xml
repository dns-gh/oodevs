<?xml version="1.0" encoding="ASCII"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="tasks.Filter" source-version="1.0.0">
    <parameters>
        <parameter name="checkpoint">
            <description>The checkpoint to enact on.</description>
            <type>
                <class name="agent.world.CheckPoint"/>
            </type>
        </parameter>
        <parameter name="intensity">
            <description>The strenght that is used to filter crowds.  The value is defined between 0 et 1. A value of '0' means &quot;no filtration at all&quot;. A value of '1' means &quot;totally blocked&quot; (the crowds that interact with the checkpoint cannot go through it).</description>
            <type>
                <simple name="numeric"/>
            </type>
            <default>
                <number>0.5</number>
            </default>
        </parameter>
    </parameters>
    <stages>
        <stage label="Filter" permanent="true">
            <skills>
                <skill name="agent.behaviors.skills.primitives.Filter">
                    <parameter name="checkpoint">
                        <value>
                            <parameter>checkpoint</parameter>
                        </value>
                    </parameter>
                    <parameter name="intensity">
                        <value>
                            <parameter>intensity</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>