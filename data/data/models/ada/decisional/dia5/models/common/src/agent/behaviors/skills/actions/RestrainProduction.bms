-- ****************************************************************************
-- To restrain production a specified ressource node
-- @params: self.params.resourceNode, a masalife resourceNode object
-- @params: self.params.quantity, the quantity to remove from the node production 
-- @returns: produceActionRunning once action is running
-- ****************************************************************************
rootnode
{
    feedbacks = { "produceActionRunning" },
    select = function( self )
        if meKnowledge:decreaseResourceProduction( self.params.resourceNode, self.params.quantity ) then
            Feedback( self.feedbacks.produceActionRunning )
        end
    end
}