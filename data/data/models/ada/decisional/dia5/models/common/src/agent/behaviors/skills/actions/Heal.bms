-- ****************************************************************************
-- To heal wounded victims within the given unit
-- @params: self.params.wounded, a masalife "wounded" object
-- @returns: healActionCompleted once action is completed
-- ****************************************************************************
rootnode
{
    feedbacks = { "healActionCompleted" },
    select = function( self )
        local Activate = Activate
        if meKnowledge:heal( self.params.wounded ) then
            Feedback( self.feedbacks.healActionCompleted )
        end
    end
}