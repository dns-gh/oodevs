-- ****************************************************************************
-- To activate filtration capability of a checkpoint object.
-- @params: self.params.checkpoint, a masalife 'checkPoint' object
-- @params: self.params.inensity, the % of filtration strength
-- @returns: filtrationActionRunning once action is running
-- ****************************************************************************
rootnode
{
    feedbacks = { "filtrationActionRunning" },
    select = function( self )
        if meKnowledge:activateFitration( self.params.checkpoint, self.params.intensity ) then
            Feedback( self.feedbacks.filtrationActionRunning )
        end
    end
}