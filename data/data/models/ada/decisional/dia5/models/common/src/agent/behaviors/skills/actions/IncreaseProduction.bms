-- ****************************************************************************
-- To increase production a specified resource node
-- @params: self.params.resourceNode, a masalife resourceNode object
-- @params: self.params.quantity, the quantity to add to the node production 
-- @returns: produceActionRunning once action is running
-- ****************************************************************************
rootnode
{
    feedbacks = { "produceActionRunning" },
    select = function( self )
        if meKnowledge:increaseResourceProduction( self.params.resourceNode, self.params.quantity ) then
            Feedback( self.feedbacks.produceActionRunning )
        end
    end
}