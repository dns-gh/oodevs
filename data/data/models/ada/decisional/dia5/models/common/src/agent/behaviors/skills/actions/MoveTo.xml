<?xml version="1.0" encoding="UTF-8"?>
<skill xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.behaviors.skills.actions.MoveTo" source-version="1.0.0">
    <description>For an agent to know how to move and reach a destination.</description>
    <parameters>
        <parameter name="destination">
            <description>The destination to reach. </description>
            <type>
                <class name="agent.world.Destination"/>
            </type>
        </parameter>
        <parameter name="urgency">
            <description>Set to true if the agent is in a hurry; false otherwise. Impacts the agent lomotion pace. </description>
            <type>
                <simple name="boolean"/>
            </type>
            <default>
                <boolean>false</boolean>
            </default>
        </parameter>
        <parameter name="pathType" nullable="true">
            <description>Type of path that agent can use to reach its target.</description>
            <type>
                <any/>
            </type>
            <default>
                <nil/>
            </default>
        </parameter>
    </parameters>
    <body-type>
        <class name="agent.world.Body"/>
    </body-type>
    <resources>
        <resource name="agent.behaviors.resources.Motion" variable="motion">
            <description>An agent cannot move to more than one destination at once. </description>
        </resource>
    </resources>
    <feedbacks>
        <feedback name="done"/>
    </feedbacks>
</skill>