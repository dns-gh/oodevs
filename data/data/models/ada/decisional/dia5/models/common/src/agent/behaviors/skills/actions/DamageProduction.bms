-- ****************************************************************************
-- To damage production a specified ressource node
-- @params: self.params.resourceNode, a masalife resourceNode object
-- @params: self.params.quantity, the quantity to remove from the node production
-- @returns: damageProductionActionCompleted once action is completed
-- ****************************************************************************
rootnode
{
    feedbacks = { "damageProductionActionCompleted" },
    select = function( self )
        if meKnowledge:damageResourceProductionCapability( self.params.resourceNode, self.params.quantity ) then
            Feedback( self.feedbacks.damageProductionActionCompleted )
        end
    end
}