-- ****************************************************************************
-- To enhance the production a specified resource node definitely
-- @params: self.params.resourceNode, a masalife resourceNode object
-- @params: self.params.quantity, the quantity to add to the node production 
-- @returns: enhanceProductionActionCompleted once action is running
-- ****************************************************************************
rootnode
{
    feedbacks = { "enhanceProductionActionCompleted" },
    select = function( self )
        if meKnowledge:enhanceResourceProductionCapability( self.params.resourceNode, self.params.quantity ) then
            Feedback( self.feedbacks.enhanceProductionActionCompleted )
        end
    end
}