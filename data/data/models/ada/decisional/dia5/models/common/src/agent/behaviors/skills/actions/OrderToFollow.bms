-- **************************************************************************** 
-- To make follow an agent
-- @params: self.params.follower, a masalife follower (teammate or non-teammate units)
-- @returns: orderToFollowActionCompleted when unit has been told to follow.
-- ****************************************************************************
rootnode
{
    feedbacks = { "orderToFollowActionCompleted" },
    select = function( self )
        if meKnowledge:orderToFollow( self.params.follower ) then
            Feedback( self.feedbacks.orderToFollowActionCompleted )
        end
    end
}