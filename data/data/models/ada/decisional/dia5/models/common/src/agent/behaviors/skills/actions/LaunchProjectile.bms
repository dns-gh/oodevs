-- ****************************************************************************
-- To launch a specified projectile on a given target 
-- @params: self.params.target, a masalife 'target' element
-- @params: self.params.launchPosition, the position from which agent unit
--          launch the projectile
-- @params: self.params.typeOfProjectile, the type of dotation to use.
-- @returns: launchActionCompleted once action is done
-- ****************************************************************************
rootnode
{
    feedbacks = { "launchActionCompleted" },
    select = function( self )
        if meKnowledge:launchProjectile( self.params.target, self.params.dotationType, self.params.quantity ) then
            Feedback( self.feedbacks.launchActionCompleted )
        end
    end
}