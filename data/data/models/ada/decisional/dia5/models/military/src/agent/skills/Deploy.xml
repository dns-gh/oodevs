<?xml version="1.0" encoding="UTF-8"?>
<skill xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.skills.Deploy" source-version="1.0.0">
    <description>For the agent, to move and deploy on the given objective.</description>
    <parameters>
        <parameter name="objective" nullable="true">
            <description>The position onto which the agent needs to deploy. If no objective is issued, the agent deploys on its own position.</description>
            <type>
                <class name="properties.Positionable"/>
            </type>
            <default>
                <nil/>
            </default>
        </parameter>
    </parameters>
    <body-type>
        <class name="world.PlatoonAlly"/>
    </body-type>
    <links>
        <link name="moveToward" skill="agent.skills.MoveToward">
            <description>To enact the decision of moving toward the given position.</description>
        </link>
        <link name="dismount" skill="agent.skills.actions.Dismount">
            <description>To enact the decision of dismounting upon reaching the deployment position.</description>
        </link>
        <link name="deploy" skill="agent.skills.actions.Deploy">
            <description>To enact the decision to self deploy once arrived on the given position.</description>
        </link>
    </links>
    <feedbacks>
        <feedback name="done">
            <description>Sent once the agent is deployed.</description>
        </feedback>
        <feedback name="deploymentDone">
            <description>Sent once the agent is deployed.</description>
        </feedback>
    </feedbacks>
</skill>