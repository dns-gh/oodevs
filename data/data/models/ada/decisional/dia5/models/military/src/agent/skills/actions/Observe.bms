-- ****************************************************************************
-- Observe toward objective.
-- Simple DirectIA actiondefined as a skill, to be used in every high level
-- skills. This skill set the main observation effort toward the objective.
-- self.params.entity: the observable element
-- ****************************************************************************
rootnode
{
    acquire = { [ skill.resources.observe ] = 1 },

    done = function( self ) end,

    select = function( self )
        self.params.entity:observeIt()
    end
}
