<?xml version="1.0" encoding="UTF-8"?>
<skill xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.skills.Bypass" source-version="1.0.0">
    <description>To bypass a set of sword objects.&#xD;
The agent visits the obstacles and bypass them one by one. Order of visit is user defined (the order is the 'entities' list order).&#xD;
In sword, bypassing an obstacle means to make it trafficable. Agents of the simulation becomes able to move through the bypassed obstacle with a limited speed of movement.</description>
    <parameters>
        <parameter name="entities" nullable="true">
            <type>
                <list>
                    <class name="properties.Bypassable"/>
                </list>
            </type>
            <default>
                <nil/>
            </default>
        </parameter>
        <parameter name="avoidObstacles">
            <type>
                <simple name="boolean"/>
            </type>
            <default>
                <boolean>true</boolean>
            </default>
        </parameter>
    </parameters>
    <links>
        <link name="bypass" skill="agent.skills.primitives.Bypass"/>
        <link name="remove" skill="agent.skills.primitives.Remove"/>
    </links>
</skill>