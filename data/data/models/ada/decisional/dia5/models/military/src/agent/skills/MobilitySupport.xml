<?xml version="1.0" encoding="UTF-8"?>
<skill xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.skills.MobilitySupport" source-version="1.0.0">
    <description>For the agent, to know how to support friend units to get rid of blocking obstacles . The agent follows the given entities. If one of them ask for support, the agent moves to the encountered obstacle and removes it.</description>
    <parameters>
        <parameter name="entities">
            <description>The entities to support.</description>
            <type>
                <list>
                    <class name="properties.Reachable"/>
                </list>
            </type>
        </parameter>
        <parameter name="distanceMin">
            <description>The distance used to follow the friend unit.</description>
            <type>
                <simple name="numeric"/>
            </type>
            <default>
                <number>100.0</number>
            </default>
        </parameter>
    </parameters>
    <links>
        <link name="StayClose" skill="agent.skills.primitives.StayClose"/>
        <link name="ByPass" skill="agent.skills.Bypass"/>
    </links>
</skill>