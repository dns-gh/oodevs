-- **************************************************************************** 
-- Move toward and deploy on objective
-- self.params.objective: the goal element
-- ****************************************************************************
rootnode
{
    connections = { skill.links.moveToward, skill.links.dismount, skill.links.deploy },
    feedbacks   = { "deployDone" },

    -- -------------------------------------------------------------------------------- 
    -- Slots
    -- --------------------------------------------------------------------------------
    deployed = function( self ) -- call by deploy action skill
        Feedback( self.feedbacks.deploymentDone )
        Feedback( self.feedbacks.done )
    end,
    done = function( self ) -- called by MoveToward skill
        self.isArrived = true
    end,

    -- -------------------------------------------------------------------------------- 
    -- Init
    -- --------------------------------------------------------------------------------
    create = function( self )
        self.isArrived = false
        if self.params.objective == NIL then
            self.objective = meKnowledge:getMyPosition() -- deploy on self position
        else
            self.objective = self.params.objective -- a position has been issued.
        end
    end,

    -- -------------------------------------------------------------------------------- 
    -- MoveToward element and start deploying on it when arrived
    -- --------------------------------------------------------------------------------
    activate = function( self )
        local Activate = Activate
        if self.isArrived then
            Activate( skill.links.dismount, 1, {} )
            Activate( skill.links.deploy, 1, {} )
        end
        Activate( skill.links.moveToward, 1, { entities = { self.objective } } )
        self.isArrived = false
    end
}