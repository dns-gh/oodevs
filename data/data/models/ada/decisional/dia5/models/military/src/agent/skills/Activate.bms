-- **************************************************************************** 
-- Activate
-- ****************************************************************************
rootnode
{
    connections = { skill.links.Activate },

    -- -------------------------------------------------------------------------------- 
    -- Feedbacks
    -- --------------------------------------------------------------------------------
    done = function( self, params, additionnalParams ) -- from skill.links.Activate connection
        Feedback( self.feedbacks.done )
    end,

    -- -------------------------------------------------------------------------------- 
    -- Element activation or send a done feedback if no elements to activate
    -- --------------------------------------------------------------------------------
    activate = function( self )
        local Activate = Activate

        if( not self.params.entities ) or ( #self.params.entities == 0 ) then
            Feedback( self.feedbacks.done )
        else
            Activate( skill.links.Activate, 1, {entities = self.params.entities, positions = self.params.positions } )
        end
    end
}