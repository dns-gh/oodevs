-- **************************************************************************** 
-- Goes to Dismount state
-- ****************************************************************************
rootnode 
{
    acquire = { [ skill.resources.mount ] = 1 },

    select = function( self )
        if meKnowledge:canDismount() then
            meKnowledge:dismount()
        end
    end,
}