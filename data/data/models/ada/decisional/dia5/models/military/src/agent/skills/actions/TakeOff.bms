-- **************************************************************************** 
-- Take off: to set the agent altitude with the 'self.params.height' value.
-- self.params.height: the wanted altitude
-- ****************************************************************************
rootnode 
{
    -- -------------------------------------------------------------------------------- 
    -- Init
    -- --------------------------------------------------------------------------------
    create = function( self )
        myself.altitude = self.params.height
        DEC_Agent_HauteurDeVol( myself.altitude )
        integration.activateSensors() -- $$$ MIA TODO A virer spé drones
        Feedback( self.feedbacks.done )
    end,
}