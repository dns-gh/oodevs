<?xml version="1.0" encoding="UTF-8"?>
<skill xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.skills.Build" source-version="1.0.0">
    <description>To build a set of sword objects. The agent moves to each of the issued element and build them. If the parameter &quot; &#xD;
&#xD;
This skill can receive an empty list of objects to build. In that case, it returns the feeback 'done'.</description>
    <parameters>
        <parameter name="entities" nullable="true">
            <description>The elements to build.</description>
            <type>
                <list>
                    <class name="properties.Buildable"/>
                </list>
            </type>
            <default>
                <nil/>
            </default>
        </parameter>
        <parameter name="withImprovement" nullable="true">
            <description>Define if the object can be improved with mines. If the mission is used to erect obstacles (engineer units), the agents can improve the objects (barricade for instance) with mines. By default, the value is 'false'.</description>
            <type>
                <simple name="boolean"/>
            </type>
            <default>
                <boolean>false</boolean>
            </default>
        </parameter>
        <parameter name="waitForReinforcement">
            <description>Define if agent needs to wait for reinforcement when it is not able to build a work.</description>
            <type>
                <simple name="boolean"/>
            </type>
            <default>
                <boolean>false</boolean>
            </default>
        </parameter>
    </parameters>
    <links>
        <link name="buildWithoutReinforcement" skill="agent.skills.BuildWithoutReinforcement"/>
        <link name="buildWithReinforcement" skill="agent.skills.BuildWithReinforcement"/>
    </links>
    <feedbacks>
        <feedback name="worksDone">
            <description>Sent once all works have been built.</description>
        </feedback>
        <feedback name="done">
            <description>Sent once all works have been built.</description>
        </feedback>
    </feedbacks>
</skill>