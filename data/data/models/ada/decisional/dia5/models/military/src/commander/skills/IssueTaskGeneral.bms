rootnode {

    done = function( self ) end,

create = function( self )
     for _, elem in pairs( self.params.entities ) do
        meKnowledge:sendTaskToPion( elem, self.params.taskName, self.params.objectives, self.params.echelon )
     end

end,

}
