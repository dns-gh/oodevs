-- **************************************************************************** 
-- To activate the logistic chains of the agent.
-- ****************************************************************************
rootnode
{
    feedbacks = { "logisticChainsActivated" },
    select = function( self )
        if meKnowledge:activateLogisticChains() then
            Feedback( self.feedbacks.logisticChainsActivated )
        end
    end
}