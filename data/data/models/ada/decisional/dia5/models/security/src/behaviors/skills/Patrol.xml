<?xml version="1.0" encoding="UTF-8"?>
<skill xmlns="http://www.masagroup.net/directia/schemas/bm" name="behaviors.skills.Patrol" source-version="1.0.0">
    <description>To visit a given set of entities in a given order. When it has visited the last entity, the agent starts over with the first one. The set of entities and the order of visit are user defined. The skill performs by activating the MoveAlong skill with the set of entities.</description>
    <parameters>
        <parameter name="entities">
            <description>The list of entities to defining a patrol round. The list must be ordered such a way that its nth element represents the nth entity to visit on the agent’s round.</description>
            <type>
                <list>
                    <class name="agent.ontology.interfaces.Destination"/>
                </list>
            </type>
        </parameter>
    </parameters>
    <goal>
        <description>Move along the target elements.</description>
    </goal>
    <links>
        <link name="moveAlong" skill="behaviors.skills.MoveAlong"/>
    </links>
</skill>