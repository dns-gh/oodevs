<?xml version="1.0" encoding="UTF-8"?>
<skill xmlns="http://www.masagroup.net/directia/schemas/bm" name="behaviors.skills.Transport" source-version="1.0.0">
    <description>For an agent to know how to transport a set of transportable entities.&#xD;
Entities are visited in the order of the list and are loaded on agent Unit. Once all elements are loaded (some entities could not be loaded), agent Unit moves toward issued destination and unload transported entities, one by one. </description>
    <parameters>
        <parameter name="entitiesToTransport">
            <description>The transportable entities to transport toward the given destination.</description>
            <type>
                <list>
                    <class name="agent.ontology.interfaces.Transportable"/>
                </list>
            </type>
        </parameter>
        <parameter name="destination">
            <description>The destination where to bring 'entitiesToTransport'.</description>
            <type>
                <class name="agent.ontology.interfaces.Destination"/>
            </type>
        </parameter>
    </parameters>
    <links>
        <link name="load" skill="behaviors.skills.primitives.Load">
            <description>To load entities to transport.</description>
        </link>
        <link name="unload" skill="behaviors.skills.primitives.Unload">
            <description>To move toward destination and then to unload loaded entities.</description>
        </link>
    </links>
    <feedbacks>
        <feedback name="done">
            <description>Sent when no more element left to transport.</description>
        </feedback>
        <feedback name="failed">
            <description>Sent once agent Unit visited all element to transport and  if it wasn't able to load any of them.</description>
        </feedback>
    </feedbacks>
</skill>