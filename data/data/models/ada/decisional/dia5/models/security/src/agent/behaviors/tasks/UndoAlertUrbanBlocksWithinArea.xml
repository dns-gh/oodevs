<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.behaviors.tasks.UndoAlertUrbanBlocksWithinArea" source-version="1.0.0">
    <description>To lift alert on population located into a set of urban blocks in a given area.&#xD;
&#xD;
If no urban block exists in the area, the mission failed and terminates.</description>
    <parameters>
        <parameter name="area">
            <description>The area with urban blocks to lift alert on.</description>
            <type>
                <class name="agent.ontology.classes.Area"/>
            </type>
        </parameter>
    </parameters>
    <stages>
        <stage label="lift alert on urban blocks" disable-when-done="true">
            <description>Move along urban blocks in the area and lift the alert on them.</description>
            <skills>
                <skill name="agent.behaviors.skills.primitives.UndoAlert" main="true">
                    <parameter name="populatedAreas">
                        <value>
                            <call method="agent.ontology.queries.GetUrbanBlocksWithinArea">
                                <parameter name="area">
                                    <value>
                                        <parameter>area</parameter>
                                    </value>
                                </parameter>
                            </call>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>