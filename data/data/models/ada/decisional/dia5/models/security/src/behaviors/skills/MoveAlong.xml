<?xml version="1.0" encoding="UTF-8"?>
<skill xmlns="http://www.masagroup.net/directia/schemas/bm" name="behaviors.skills.MoveAlong" source-version="1.0.0">
    <description>To follow a path defined as an ordered set of entities by the user. The skill performs by activating the MoveToward skill with one entity at a time, in the specified order. </description>
    <parameters>
        <parameter name="entities">
            <description>The list of entities defining the path. The list must be ordered such a way that its nth element represents the nth entity on the agent’s path</description>
            <type>
                <list>
                    <class name="agent.world.Destination"/>
                </list>
            </type>
        </parameter>
        <parameter name="goToNextTargetAfterInstallation">
            <description>Define if agent moves to next target only after being installed on current target entity.&#xD;
&#xD;
If value is set to 'true', each time the agent reaches a target entity, it occupies its position until it is installed on it. After then, it decides to reach the next target entity. Being installed takes time, as it is defined in the &quot;Stances&quot; section of the authoring tool.&#xD;
&#xD;
Default value is 'false' (the agent moves to the next target once it is reached).</description>
            <type>
                <simple name="boolean"/>
            </type>
            <default>
                <boolean>false</boolean>
            </default>
        </parameter>
    </parameters>
    <goal>
        <description>Reach each objectives in a specific order.</description>
    </goal>
    <links>
        <link name="reach" skill="agent.behaviors.skills.actions.MoveTo">
            <description>To enact the decision to reach each given entities.</description>
        </link>
    </links>
</skill>