<?xml version="1.0" encoding="UTF-8"?>
<query xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.ontology.queries.GetAllReachableDestinationInArea" source-version="1.0.0">
    <description>Return all reachable positions (&quot;destinations&quot;) within the given aera. The returned positions are:&#xD;
- the crossroads,&#xD;
- intersections between roads and the given area,&#xD;
- all kind of objects with the &quot;protection&quot; capacity (see definition of objects in authoring tool).</description>
    <parameters>
        <parameter name="area">
            <type>
                <class name="agent.ontology.classes.Area"/>
            </type>
        </parameter>
    </parameters>
    <target name="keyTerrain">
        <type>
            <class name="agent.world.Destination"/>
        </type>
    </target>
</query>