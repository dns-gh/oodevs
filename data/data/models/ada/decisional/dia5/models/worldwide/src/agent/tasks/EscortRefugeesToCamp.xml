<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.tasks.EscortRefugeesToCamp" source-version="1.0.0" knowledge-class="agent.tasks.knowledges.EscortRefugeesToCamp">
    <description>For the agent, to know how to escort a set of refugees units toward a refugees camp.&#xD;
&#xD;
The agent decides to move toward each given units, from the nearest to the furthest. Once a unit has been reached, the agent loads it (magic tranportation).&#xD;
&#xD;
Once all units has been loaded, they are tranported to the refugees camp. Once it is reached, the units are unloaded into the camp.</description>
    <parameters>
        <parameter name="camp">
            <description>The refugees camp inside which the agent has to escort the refugees units.</description>
            <type>
                <class name="properties.Camp"/>
            </type>
        </parameter>
        <parameter name="entities">
            <description>The refugees units to be escorted to the camp.</description>
            <type>
                <list>
                    <class name="properties.Loadable"/>
                </list>
            </type>
        </parameter>
    </parameters>
    <goal>
        <description>To transport a set of refugees units to a refugees camp.</description>
    </goal>
    <stages>
        <stage label="moving toward refugees units">
            <skills>
                <skill name="agent.skills.primitives.Load" main="true">
                    <parameter name="entities">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
        <stage label="moving toward camp" disable-when-done="true">
            <skills>
                <skill name="agent.skills.DischargeIn" main="true">
                    <parameter name="elements">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                    <parameter name="camp">
                        <value>
                            <parameter>camp</parameter>
                        </value>
                    </parameter>
                </skill>
                <skill name="agent.skills.UnloadTransportedUnits">
                    <parameter name="entities">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>