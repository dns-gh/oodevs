<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.tasks.Follow" source-version="1.0.0" knowledge-class="agent.tasks.knowledges.Follow">
    <description>For the agent to know how to follow a set of moving agents or crowds. The agent chooses the best positions to stay close to a maximum of the given entities. To be followed, an agent must be operational. If all the issued objectives are not operational, the mission terminates with a 'failed' status.</description>
    <parameters>
        <parameter name="objectives">
            <description>The entities to follow.</description>
            <type>
                <list>
                    <class name="properties.Motile"/>
                </list>
            </type>
        </parameter>
    </parameters>
    <stages>
        <stage label="following units" permanent="true">
            <skills>
                <skill name="agent.skills.Follow">
                    <parameter name="entities">
                        <value>
                            <parameter>objectives</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>