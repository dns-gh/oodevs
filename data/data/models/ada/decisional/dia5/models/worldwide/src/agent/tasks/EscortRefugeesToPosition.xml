<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.tasks.EscortRefugeesToPosition" source-version="1.0.0" knowledge-class="agent.tasks.knowledges.EscortRefugeesToPosition">
    <description>For the agent, to know how to escort a set of refugees units toward a given location.&#xD;
&#xD;
The agent decides to move toward each given units, from the nearest to the furthest. Once a unit has been reached, the agent loads it (magic tranportation).&#xD;
&#xD;
Once all units has been loaded, they are tranported to the given location. Then, they are unloaded.</description>
    <parameters>
        <parameter name="position">
            <description>The location into which the agent has to escort the refugees units.</description>
            <type>
                <class name="properties.Loading"/>
            </type>
        </parameter>
        <parameter name="entities">
            <description>The refugees units to be unloaded onto the given location.</description>
            <type>
                <list>
                    <class name="properties.Loadable"/>
                </list>
            </type>
        </parameter>
    </parameters>
    <goal>
        <description>To transport refugees units to a given location.</description>
    </goal>
    <stages>
        <stage label="moving toward refugees units">
            <skills>
                <skill name="agent.skills.primitives.Load" main="true">
                    <parameter name="entities">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
        <stage label="moving to location" disable-when-done="true">
            <skills>
                <skill name="agent.skills.primitives.Unload" main="true">
                    <parameter name="entities">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                    <parameter name="positions">
                        <value>
                            <list>
                                <parameter>position</parameter>
                            </list>
                        </value>
                    </parameter>
                </skill>
                <skill name="agent.skills.UnloadTransportedUnits">
                    <parameter name="entities">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>