<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.tasks.TransportACrowd" source-version="1.0.0" knowledge-class="agent.tasks.knowledges.TransportACrowd">
    <description>To transport a 'Crowd' agent toward a given destination. &#xD;
Depending on its crowd transport capacity, agent will make as many round trips as necessary between 'loadingPoint' and the 'destination'. &#xD;
Because the agent can only transports crowds' concentrations, this mission needs a loading position to indicate which concentration to transport. Indeed, a crowd can be compouded with several separated concentrations. Only one concentration can be transported at a time. The agent will transport the nearest concentration from the &quot;loadingPoint&quot; parameter.</description>
    <parameters>
        <parameter name="crowd">
            <description>The crowd to transport.</description>
            <type>
                <class name="properties.Transportable"/>
            </type>
        </parameter>
        <parameter name="loadingPoint">
            <description>The crowd's concentration position onto which the agent loads the elements of the crowd.</description>
            <type>
                <class name="properties.Loading"/>
            </type>
        </parameter>
        <parameter name="destination">
            <description>The destination onto which the agent must bring the crowd.</description>
            <type>
                <class name="properties.Loading"/>
            </type>
        </parameter>
        <parameter name="meetingPoint">
            <description>The position to reach after the crowd transportation.</description>
            <type>
                <class name="properties.Reachable"/>
            </type>
        </parameter>
    </parameters>
    <stages>
        <stage label="crowd transportation">
            <skills>
                <skill name="agent.skills.TransportACrowd" main="true">
                    <parameter name="crowd">
                        <value>
                            <parameter>crowd</parameter>
                        </value>
                    </parameter>
                    <parameter name="loadingPoint">
                        <value>
                            <parameter>loadingPoint</parameter>
                        </value>
                    </parameter>
                    <parameter name="destination">
                        <value>
                            <parameter>destination</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
        <stage label="moving to meeting position" disable-when-done="true">
            <skills>
                <skill name="agent.skills.MoveAlong" main="true">
                    <parameter name="entities">
                        <value>
                            <list>
                                <parameter>meetingPoint</parameter>
                            </list>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>