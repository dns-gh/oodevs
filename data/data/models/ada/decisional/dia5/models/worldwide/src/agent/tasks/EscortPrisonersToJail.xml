<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.tasks.EscortPrisonersToJail" source-version="1.0.0" knowledge-class="agent.tasks.knowledges.EscortPrisonersToJail">
    <description>For the agent, to know how to escort a set of surrendered units toward a prisoners camp.&#xD;
&#xD;
The agent decides to move toward each given units, from the nearest to the furthest. Once a unit has been reached, the agent loads it (magic tranportation).&#xD;
&#xD;
Once all units has been loaded, they are tranported to the prisoners camp. Once it is reached, the units are unloaded into the camp. They become 'prisoner' units.</description>
    <parameters>
        <parameter name="camp">
            <description>The prisoners camp inside which the agent has to escort the surrendered units.</description>
            <type>
                <class name="properties.Camp"/>
            </type>
        </parameter>
        <parameter name="entities">
            <description>The surrendered units to be jailed into the camp.</description>
            <type>
                <list>
                    <class name="world.Platoon"/>
                </list>
            </type>
        </parameter>
    </parameters>
    <goal>
        <description>To transport a set of surrendered units to a prisoners camp.</description>
    </goal>
    <stages>
        <stage label="moving toward surrendered units">
            <skills>
                <skill name="agent.skills.TransportPrisoners" main="true">
                    <parameter name="platoons">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
        <stage label="moving to camp" disable-when-done="true">
            <skills>
                <skill name="agent.skills.DischargeIn" main="true">
                    <parameter name="camp">
                        <value>
                            <parameter>camp</parameter>
                        </value>
                    </parameter>
                    <parameter name="elements">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                </skill>
                <skill name="agent.skills.UnloadTransportedUnits">
                    <parameter name="entities">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>