<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.tasks.MoveAlong" source-version="1.0.0" knowledge-class="agent.tasks.knowledges.MoveAlong">
    <description>For the agent, to move along a path defined as an ordered set of 'objectives' by the user. </description>
    <parameters>
        <parameter name="objectives">
            <description>The list of entities defining the path. The list must be ordered such a way that its nth element represents the nth entity on the agent’s path.</description>
            <type>
                <list>
                    <class name="properties.Reachable"/>
                </list>
            </type>
        </parameter>
    </parameters>
    <stages>
        <stage label="movement" disable-when-done="true">
            <skills>
                <skill name="agent.skills.MoveAlong" main="true">
                    <parameter name="entities">
                        <value>
                            <parameter>objectives</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>