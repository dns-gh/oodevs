<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.tasks.Remove" source-version="1.0.0" knowledge-class="agent.tasks.knowledges.Remove">
    <description>To remove a set of sword objects.&#xD;
The agent visits the obstacles and removes them one by one. Order of visit is user defined (the order is the 'entities' list order).&#xD;
&#xD;
 In sword, removing an obstacle is to deconstruct it (the construction level decreases) and make it disappear.</description>
    <parameters>
        <parameter name="entities">
            <description>The set of entities to remove (objects such as obstacles, intallations, bridges etc.).</description>
            <type>
                <list>
                    <class name="properties.Removable"/>
                </list>
            </type>
        </parameter>
        <parameter name="meetingPoint" nullable="true">
            <description>The meeting position to reach upon works termination.</description>
            <type>
                <class name="properties.Reachable"/>
            </type>
        </parameter>
    </parameters>
    <stages>
        <stage label="moving to obstacles">
            <skills>
                <skill name="agent.skills.RemoveWithReinforcement" main="true">
                    <parameter name="avoidObstacles">
                        <value>
                            <boolean>true</boolean>
                        </value>
                    </parameter>
                    <parameter name="entities">
                        <value>
                            <parameter>entities</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
        <stage label="moving to meeting position" disable-when-done="true">
            <skills>
                <skill name="agent.skills.MoveToward" main="true">
                    <parameter name="entities">
                        <value>
                            <list>
                                <parameter>meetingPoint</parameter>
                            </list>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>