<?xml version="1.0" encoding="UTF-8"?>
<task xmlns="http://www.masagroup.net/directia/schemas/bm" name="agent.tasks.DeployAndActivateLogisticChains" source-version="1.0.0" knowledge-class="agent.tasks.knowledges.DeployAndActivateLogisticChains">
    <description>For the agent, to self deploy on a given position and then activcate its logistic functions. &#xD;
The agent moves to the position, deploys onto it and then enables its logistic chains. </description>
    <parameters>
        <parameter name="deploymentPosition" nullable="true">
            <description>Position pour le déploiement</description>
            <type>
                <class name="properties.Positionable"/>
            </type>
            <default>
                <nil/>
            </default>
        </parameter>
    </parameters>
    <stages>
        <stage label="deploying on position" permanent="true">
            <skills>
                <skill name="agent.skills.DeployAndActivateLogisticChains">
                    <parameter name="objective">
                        <value>
                            <parameter>deploymentPosition</parameter>
                        </value>
                    </parameter>
                </skill>
            </skills>
        </stage>
    </stages>
</task>