-- ****************************************************************************
-- Simple internal skill activated by LOG fragorder. Ask the agent
-- to get tugs to an other agent.
-- ****************************************************************************
rootnode
{
    done = function( self ) end,
    
    create  = function( self )
       meKnowledge:GetTugs( self.params.pionRenforce, self.params.nbrRemorqueurs )
    end,
}
