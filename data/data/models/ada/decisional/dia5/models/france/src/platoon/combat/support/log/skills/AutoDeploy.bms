rootnode 
{
    connections = { skill.links.activateLogisticChains },

    done = function( self )
    end,
    
    activate = function( self )
        local Activate = Activate
        Activate( skill.links.activateLogisticChains, 0.01 )
    end,
}