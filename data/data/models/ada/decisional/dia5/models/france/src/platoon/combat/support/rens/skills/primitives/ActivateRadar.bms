rootnode
{
    acquire = { [ skill.resources.radar ] = 1 },
    
    done = function( self ) end,
    
    select = function( self )
        self.params.area:activateRadar( self.params.typeRadar )
    end,
}