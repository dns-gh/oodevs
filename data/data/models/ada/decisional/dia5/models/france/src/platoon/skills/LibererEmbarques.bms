rootnode
{
    done = function( self ) end,

        destroy = function( self )
        if #self.params.entities ~= 0 then
            for _, entity in pairs ( self.params.entities ) do
                if entity:canUnloadIt() then
                    entity:unloadIt()
                end
            end
        end
    end,
}
