rootnode {

done = function( self ) end,

create = function( self )
    local parameters = {position = self.params.position, allie = self.params.allie, objective = self.params.objective,
                        munition = self.params.munition, interventionType = self.params.interventionType}
    meKnowledge:sendTaskToPion(self.params.tireur,self.params.task, parameters, eEtatEchelon_None )
end,
}