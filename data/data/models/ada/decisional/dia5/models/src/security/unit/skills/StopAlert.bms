rootnode {

    done = function( self ) end,

    activate = function( self )
        if masalife.brain.core.isTaskRunning( security.unit.tasks.Alert ) then
            meKnowledge.mustStopAlertAndGotoGarage = true
        end
	    Feedback( self.feedbacks.done )
    end

}