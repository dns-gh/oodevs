rootnode {

    done = function( self ) end,

create = function( self )
     for _, elem in pairs( self.params.entities ) do
        meKnowledge:sendTaskToPion( elem.entity, elem.taskName, elem.params, self.params.echelon )
     end
 
end,

}
