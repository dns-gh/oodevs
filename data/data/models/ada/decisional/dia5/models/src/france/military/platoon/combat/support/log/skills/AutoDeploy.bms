rootnode {
    connections = { skill.links.Deploy },

    create = function( self )
    end,
    
    done = function( self )
    end,
    
    activate = function( self )
        local Activate = Activate
        Activate( skill.links.Deploy, 0.01 )
    end,
}