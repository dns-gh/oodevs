pragma "once"

node "BEH_Defaut"
{}

local configTable = {   eventmanager_plugin = 
                        {
                            BEH_Defaut = {},
                        },
                        connections =
                        {
                        } }

brain "name" ( configTable )

masalife.brain.core = {}
masalife.brain.core.startTask = StartEvent
masalife.brain.core.stopTask = StopEvents
