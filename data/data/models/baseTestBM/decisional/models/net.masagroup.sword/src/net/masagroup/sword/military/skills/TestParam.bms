rootnode
{
    connections =
    {
    };

    activate = function( self )
    	if( self.params.Point_ ) then
    		BreakForDebug( "TestParam Knowledges created: Point")
        end
        if( self.params.PointList_ ) then
        	BreakForDebug( "TestParam Knowledges created: PointList")
        end
    end
}