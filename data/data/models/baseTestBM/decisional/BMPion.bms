pragma "once"
--@TODO mutualiser les parties indépendantes  du modèle
include "ToolsFunctions.lua"
include "Integration.lua"

local libPath = select(1,...)
local role    = select(2,...)
behavior_model.loadModel( role, { libPath .. "/models/libs/core", libPath .. "/models/libs/base", libPath .. "/models/net.masagroup.sword/src", libPath .. "/models/net.masagroup.testSword/src" } )
